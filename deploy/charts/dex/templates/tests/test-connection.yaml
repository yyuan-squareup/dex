apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "dex.fullname" . }}-test-connection"
  labels:
    {{- include "dex.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "dex.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
